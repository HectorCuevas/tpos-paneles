<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rasoftec.tpos2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="com.rasoftec.ApplicationTpos"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/provider_paths" />

        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lista">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/busqueda2" />
        </activity>
        <activity android:name=".nodo_lista" />
        <activity android:name=".cobro" />
        <activity android:name=".venta">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/busqueda2" />
        </activity>
        <activity android:name=".menu_principal" />
        <activity android:name=".activities.LocationActivity" />
        <activity android:name=".activities.PhotoActivity" />
        <activity android:name=".activities.DetailActivity" />
        <activity android:name=".Preferences.LocationPreferences" />
        <activity android:name=".activities.ChartActivity" />
        <activity android:name=".activities.TableActivity" />
        <activity android:name=".activities.FacturasPorEnviarActivity" />
        <activity android:name=".activities.EncabezadoFacturaActivity" />
        <activity android:name=".activities.InventarioActivity"></activity>
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>